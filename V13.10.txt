V13.10
2017-2-23
说明：
1，修复OBD累加计算里程的BUG：原来首次上点默认里程是0，累加计算里程从0开始，从而导致有里程上传，而不是上传里程0XFFFFFF;而且读码失败也会清楚里程。修复后采用累加计算里程方式，第一次上电上传0XFFFFFF,提示矫正里程。
2，修复CAN总线获取里程中，如果高字节在低字节位时，高低字节互换错误的BUG。
3，修复读取故障码个数过多，导致无法上传的BUG，默认接收15帧数据，现在改成任意接收。